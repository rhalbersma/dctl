#include "Notation.h"

namespace dctl {
namespace notation {

template<> struct Format<rules::Czech>          { typedef algebraic type; };

}       // namespace notation
}       // namespace dctl
